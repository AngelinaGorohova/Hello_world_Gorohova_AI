#!/bin/bash

printf "%-25s %-10s\n" "Файловая система" "Заполнение"
echo "------------------------------------------------"

df -h | awk -F '[ %]+' 'NR > 1 {
    usage = $5; 
    printf "%-25s %-10s\n", $1, usage

    if (usage > 90) {
        printf "ВНИМАНИЕ: КРИТИЧЕСКИЙ УРОВЕНЬ"
    }
    #для перехода на новую строку
    print "" 
}'